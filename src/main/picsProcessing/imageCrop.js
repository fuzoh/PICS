import fs from 'fs'
import path from 'path'
import { nativeImage } from 'electron'

export default {
  /*
  | generateThumbnail
  |
  | Gets the date of all the pics and rename it
  | @param object path
  | @param function success callback
  | @param function error callback
  */
  generateThumbnail (path, success) {

    success()

  }
}