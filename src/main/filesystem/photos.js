// ****************************
// filesystem -> photos
// this files inject in the app all the methods to work with the photos (load files, write, update names)

let fs = require('fs')

module.exports = {
  
}